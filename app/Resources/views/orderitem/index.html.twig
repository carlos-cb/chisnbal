{% extends 'ChisnbalBundle:BackEnd:index.html.twig' %}

{% block menuslide %}
    <ul class="nav nav-sidebar">
        <li><a href="{{ path('chisnbal_homepage_admin') }}">总览 <span class="sr-only">(current)</span></a></li>
        <li><a href="#">报告</a></li>
        <li><a href="#">分析</a></li>
    </ul>
    <ul class="nav nav-sidebar">

    </ul>
{% endblock %}

{% block content %}
    <h1 class="sub-header">订单货物列表</h1>
    <h3>订单编号：{{ orderInfo.id }}</h3>
    <table class="table table-bordered table-striped table-hover">
        <thead>
        <tr>
            <th>ID</th>
            <th>数量(包/箱)</th>
            <th>产品编号</th>
        </tr>
        </thead>
        <tbody>
        {% for orderItem in orderItems %}
            <tr>
                <td>{{ orderItem.id }}</a></td>
                <td>{{ orderItem.quantity }}</td>
                <td>{{ orderItem.product.code }}</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
{% endblock %}